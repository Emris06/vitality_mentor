# iSpring mock — production env template. The mock is deliberately tiny;
# only PORT and HOST are honored beyond the failure-injection knob.
PORT=$PORT
HOST=0.0.0.0
# Keep at 0 for the demo. Bump to ~0.05 during chaos tests to exercise the
# API's retry path against a flaky LMS.
MOCK_FAIL_RATE=0
