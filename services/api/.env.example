# API service config
NODE_ENV=development
API_HOST=0.0.0.0
API_PORT=4000
API_LOG_LEVEL=info

# Allowed origin for the web app
WEB_ORIGIN=http://localhost:5173

# Postgres (pgvector enabled in init.sql)
DATABASE_URL=postgres://vitality:vitality@localhost:5433/vitality

# Redis (cache + streams)
REDIS_URL=redis://localhost:6379

# AI service (internal)
AI_SERVICE_URL=http://localhost:8000

# iSpring LMS — use local mock for dev
ISPRING_BASE_URL=http://localhost:4010
ISPRING_API_KEY=dev-mock-key

# Supabase auth — verify JWTs issued by your Supabase project.
# Leave SUPABASE_JWT_SECRET blank to disable JWT auth (cookie fallback only).
# Get these from Supabase Studio → Project Settings → API.
NEXT_PUBLIC_SUPABASE_URL=https://hufrveqdxmekowiwuvhe.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_iIMg9HwpES_isAHPbQPKPw_msaL2UBr